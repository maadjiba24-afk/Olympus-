# Olympus release-signing public keys (Ed25519, hex) — ONE KEY PER LINE.
# `olympus verify` trusts ONLY the keys listed here (plus OLYMPUS_PINNED_PUBKEY)
# for release manifests: a manifest signed with any other key is rejected, not
# merely checked for internal consistency.
# Rotation: append the NEW key while the old one still verifies history, switch
# the signing seed, then remove the old key after the overlap window — see
# docs/SIGNING.md. Derived from the secret signing seed (held as a GitHub
# Actions secret); see RELEASING.md for how a release is cut.
350f970ac5159b30f6736c124a1e468cd1cc82ddd73cb24799057c5c3b0b0336
