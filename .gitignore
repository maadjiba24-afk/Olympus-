__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
memory/
keys.sh
# the signing seed must never be committed, wherever keygen wrote it
signing_seed
deploy/.env
deploy/.env.staging
olympus/verification.json

# Python build artifacts (never commit)
build/
dist/
*.egg-info/
venv312/

# CI-log captures. `git add -A` has swept these into commits three times
(win_failure.txt, searchlive_failure.txt, webkit_failure.txt); they are
scratch evidence, never source.
*_failure.txt
